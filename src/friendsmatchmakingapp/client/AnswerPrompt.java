package friendsmatchmakingapp.client;

import javax.swing.*;

public class AnswerPrompt {
    private JPanel panel1;
    private JTextField textField1;
    private JButton submitAnswerButton;

    // 1) Submit request to the server (start timer countdown for drawing), with field being the answer
    // 2) Post request to all users
    // 3) Close the window and enable drawing for the current user
}
